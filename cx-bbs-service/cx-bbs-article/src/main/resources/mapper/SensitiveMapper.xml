<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cx.article.mapper.SensitiveMapper">

    <select id="selectSensitives" resultType="com.cx.model.article.pojos.Sensitive">
        SELECT sensitives FROM  `sensitive`
    </select>
</mapper>
